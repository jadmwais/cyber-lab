# 🔓 Brute Force Simulator (Python)

This tool simulates a brute-force attack on a password using lowercase letters (`a-z`).  
It shows how long and how many attempts it takes to crack a password of a given length.

---

## ▶️ How to Run

```bash
python brute_force.py
